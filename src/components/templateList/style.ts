import styled from 'styled-components';

const Container = styled.div`
    display: flex;
    gap: 100px;

    div {
        cursor: pointer;
        width: fit-content;
    }
`;

export { Container };
